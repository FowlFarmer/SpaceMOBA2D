using UnityEngine;

public class ColorDetector : MonoBehaviour
{
    /*public Color targetColor = Color.black; // The color to compare against

    public static bool DetectColor(int x, int y)
    {
        SpriteRenderer targetSpriteRenderer = GetComponent<SpriteRenderer>();
        Texture2D texture = targetSpriteRenderer.sprite.texture;
        Color pixelColor = texture.GetPixel(x, y);

        // Compare the pixel color with the target color
        if (pixelColor.Equals(targetColor))
        {
            return true;
        }

        return false;
    }*/
}
